import React from "react";
import LocationsContainer from "@/components/bases/LocationsContainer";

const LocationsAll = () => {
  return (
    <React.Fragment>
      <LocationsContainer languageCode="all" />
    </React.Fragment>
  );
};

export default LocationsAll;
