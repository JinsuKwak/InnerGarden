import React from "react";
import LocationsContainer from "@/components/bases/LocationsContainer";

const LocationsEnglish = () => {
  return (
    <React.Fragment>
      <LocationsContainer languageCode="en" />
    </React.Fragment>
  );
};

export default LocationsEnglish;
